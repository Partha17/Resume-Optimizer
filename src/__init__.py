"""Resume Optimizer pipeline package.

Modules are imported lazily by the notebook; do not eagerly import heavy
dependencies here (sentence-transformers, jobspy, etc.) to keep ``import src``
cheap.
"""
